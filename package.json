{
  "name": "utils-root",
  "private": true,
  "version": "0.0.1",
  "devDependencies": {
    "@changesets/cli": "^2.26.2",
    "rimraf": "^5.0.1",
    "turbo": "latest",
    "typescript": "^5.0.2"
  },
  "scripts": {
    "build": "turbo run build --filter=./packages/*",
    "watch": "turbo run watch",
    "release": "pnpm build && changeset publish"
  },
  "//pnpm": {
    "overrides": {
      "preact-test>components-for-test>react": "preact",
      "preact-test>components-for-test>react-dom": "preact",
      "preact-test>components-for-test>@preact/signals-react": "@preact/signals"
    }
  },
  "dependencies": {
    "@preact/signals": "^1.1.4",
    "preact": "^10.15.1"
  }
}
