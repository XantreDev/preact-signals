{
  "name": "preact-test",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "watch": "pnpm dev",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "preact": "^10.15.1",
    "@preact/signals": "^1.1.4",
    "components-for-test": "workspace:*",
    "@preact-signals/components": "workspace:*"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.5.0",
    "vite": "^4.4.0"
  }
}
