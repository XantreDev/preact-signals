{
  "$schema": "https://v2-8-20.turborepo.dev/schema.json",
  "globalEnv": ["CARGO_DEBUG", "SKIP_RUST"],
  "tasks": {
    "build": {
      "outputs": ["dist/**"],
      "dependsOn": ["^build"]
    },
    "watch": {
      "persistent": true,
      "outputs": ["dist/**"]
    },
    "dev": {
      "dependsOn": ["^build"],
      "persistent": true
    },
    "test": {
      "persistent": true
    },
    "lint": {
      "dependsOn": ["^build", "build"],
      "outputs": ["*.tsbuildinfo"]
    }
  }
}
